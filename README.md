# TKB Manager

A minimal React + Tailwind app for parsing raw timetable text, selecting theory/practice groups, detecting schedule conflicts, and exporting the timetable as an image.

## Run

```bash
npm install
npm run dev
```
